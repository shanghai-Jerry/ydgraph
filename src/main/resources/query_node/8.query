# 统计公司指定部门下的总候选人数量
 {
     var(func:uid(%s)) {
       name
       company_dept %s {
         name
         total_candidate as count(~candidate_dept)
       }
     }
     query(func:uid(total_candidate)) {
      name
      total:val(total_candidate)
    }
  }