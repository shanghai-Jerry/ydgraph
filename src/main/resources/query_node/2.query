{
   var(func:uid(%s)) {
     company_dept %s {
       ~candidate_dept @groupby(degree_node) {
         a as count(uid)
       }

     }

   }
   query(func:uid(a),orderdesc:val(a)) {
     uid
     name
     total:val(a)
   }
 }