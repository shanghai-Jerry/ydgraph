# 按公司统计各部门下的候选人总数
{
    var(func:uid(%s)) {
       uid
       name
       company_dept {
         name
         total_number as count(~candidate_dept)
       }
     }
    query(func:uid(total_number), orderdesc:val(total_number)) {
     name
     total:val(total_number)
   }
 }


